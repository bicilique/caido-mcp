# Finding Workflow

Before `caido_create_finding`, require an affected asset, project/request evidence IDs, identity and ownership context, minimal mutation, control versus test result, reproducibility, impact, confidence, and limitations. Search existing findings first. If any proof element is missing, report a hypothesis and the next safe test instead of creating a confirmed finding.
